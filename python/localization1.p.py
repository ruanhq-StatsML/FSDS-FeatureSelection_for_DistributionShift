#Chronoberg Synthetic DID or something like this:
from benchmark_m
import azcausal
import causalpy as cp
import synthetic_control

#loading the chronoberg dataset and then localize the distribution shift across:

